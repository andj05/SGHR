@model IEnumerable<SGHR.Application.Dtos.Servicios.ServiciosDto>

@{
    ViewData["Title"] = "Index";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Servicios</title>
    <link rel="stylesheet" href="path/to/your/css/file.css">
</head>
<body>
    <h1>Servicios</h1>

    <p>
        <a href="/Servicios/Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>Id Servicio</th>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Estado</th>
                <th>Change Date</th>
                <th>Change User</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td><span>@item.IdServicio</span></td>
                    <td><span>@item.Nombre</span></td>
                    <td><span>@item.Descripcion</span></td>
                    <td><span>@item.Estado</span></td>
                    <td><span>@item.ChangeDate</span></td>
                    <td><span>@item.ChangeUser</span></td>
                    <td>
                        <a href="/Servicios/Edit/@item.IdServicio">Edit</a> |
                        <a href="/Servicios/Details/@item.IdServicio">Details</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
